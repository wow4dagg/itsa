#include <stdio.h>

int main()
{
	int array[200];	
	int num , n = 0 ;
	int i = 0;
	while(scanf("%d",&num)!=EOF)
	{
		array[n] = num ; 
		n++ ; 	
	}
	for ( i = n-1 ; i >= 0 ; i--)	
	{
		if(i == 0)
			printf("%d\n",array[i]);
		else
		printf("%d ",array[i]);
	}
	return 0;
}
